# Upload this M1C patch

Upload the contents of this directory to the root of the GitHub repository, preserving paths.

Recommended commit / pull-request title:

`Milestone 1C - networking hardening`

Do not merge until both GitHub Actions checks are green.
